# Runtime data (DB, digests, logs) — regenerated by each scan
data/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets — if you add SMTP credentials, keep them out of git
config.local.json

# Local Claude Code tooling config (permission allowlist, preview launch config)
.claude/
